{
  "name": "ntuaha.github.io",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "data:build": "node scripts/transform-data-md.mjs",
    "build": "npm run data:build && vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "publish": "npm run build && npm run deploy"
  },
  "dependencies": {
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "gh-pages": "^6.3.0",
    "vite": "^6.2.0"
  }
}
